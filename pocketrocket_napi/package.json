{
    "package": "pocketrocket_napi",
    "devDependencies": {
        "@napi-rs/cli": "^1.0.0"
    },
    "napi": {
        "name": "pocketrocket"
    },
    "scripts": {
        "build": "napi build --release",
        "build:debug": "napi build"
    }
}